make moonwave.toml and the folder to carry its png/files
make .github and its issue templates

rather than having a .lua and .luau file, check compatibility at runtime for each component and only fallback to simpler means if necessary.

fill out selene.toml and stylua.toml ( for selene also get the globals done in .globals perferrebly, name methods based off sunc)
